diff --git a/src/data/dataset/coco_dataset.py b/src/data/dataset/coco_dataset.py
--- a/src/data/dataset/coco_dataset.py
+++ b/src/data/dataset/coco_dataset.py
@@
         else:
-            image, target = self.prepare(image, target)
+            # COCO category IDs are 1-based, whereas detector class indices
+            # are 0-based. Map declared categories to contiguous IDs.
+            image, target = self.prepare(
+                image, target, category2label=self.category2label
+            )
diff --git a/src/solver/det_engine.py b/src/solver/det_engine.py
--- a/src/solver/det_engine.py
+++ b/src/solver/det_engine.py
@@
         res = {target['image_id'].item(): output for target, output in zip(targets, results)}
         if coco_evaluator is not None:
-            coco_evaluator.update(res)
+            # COCO expects dataset category IDs; the detector emits contiguous
+            # zero-based labels for non-COCO datasets.
+            label2category = data_loader.dataset.label2category
+            coco_res = {}
+            for target, output in zip(targets, results):
+                coco_labels = torch.tensor(
+                    [label2category[int(x.item())] for x in output["labels"].flatten()],
+                    device=output["labels"].device,
+                ).reshape(output["labels"].shape)
+                coco_res[target["image_id"].item()] = {
+                    "labels": coco_labels,
+                    "boxes": output["boxes"],
+                    "scores": output["scores"],
+                }
+            coco_evaluator.update(coco_res)
